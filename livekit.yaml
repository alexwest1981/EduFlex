port: 7880
logging:
  json: false
  level: info

rtc:
  tcp_port: 7881
  udp_port: 7882
  use_external_ip: true

turn:
  enabled: true
  domain: livekit.eduflexlms.se
  udp_port: 3478
  # Integrated TURN relay configuration

keys:
  # devkey: secret is set via environment variable in docker-compose
