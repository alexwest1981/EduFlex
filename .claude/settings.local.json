{
  "permissions": {
    "allow": [
      "Bash(docker-compose restart:*)",
      "Bash(docker-compose logs:*)",
      "Bash(docker-compose exec frontend sh -c \"rm -rf node_modules/.vite && pkill -f vite\")",
      "Bash(docker-compose exec:*)"
    ]
  }
}
